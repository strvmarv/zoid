# Local agent-tooling scratch (not part of the project source)
.superpowers/
.claude/
.worktrees/

# Rust build output
/target
spikes/rust-spike/target

# .NET spike build output
spikes/dotnet-spike/bin
spikes/dotnet-spike/obj
spikes/dotnet-spike/out-aot

# misc
*.log

# zoid local session data + local (gitignored) config.local.toml;
# project ./.zoid/config.toml IS committable (the shared repo override, §7.1).
/.zoid/*
!/.zoid/config.toml

# macOS
.DS_Store
rust_out
*.snap.new
spikes/*/target/
spikes/*/*/target/
spikes/embed-rerank-eval/target*/
